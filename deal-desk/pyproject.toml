[project]
name = "{{ project_slug }}"
version = "0.1.0"
description = "A custom React deal desk with named chat threads."
requires-python = ">=3.13"
dependencies = [
    "cpsl>=0.3.107",
]

[tool.setuptools]
py-modules = ["app"]

[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"
